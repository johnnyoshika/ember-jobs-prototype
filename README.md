# Ember-jobs-prototype

## Update application from source

* `git pull`
* `npm install`
* `bower install`

## Run application

* `ember s` to use http-mock
* `DISABLE_MOCK=true ember s --proxy http://localhost:3000` to use proxy
